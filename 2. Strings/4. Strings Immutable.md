
---

# 📘 **Notes: Immutable Strings**
---

## ✅ **Muatability**
* Strings are immutable while StringBuffer and StringBuilder are mutable.
```java
String s = new String("Hello");
s.concat(" World!");
System.out.println(s);
```
* `new String("Hello");` creates a new String Object in heap.
* `s.concat(" World!");` creates another String object in heap as Strings are immutable. 
* Since, `s` is reference of first String Object therefore `System.out.println(s);` will print out `“Hello”`.
---

## ✅ **StringBuffer or StringBuilder**
```java
StringBuffer sb = new StringBuffer("Hello");
sb.append(" World!");
System.out.println(sb);
```
* `StringBuffer sb = new StringBuffer("Hello");` creates a new object in heap. 
* Since StringBuffer class is mutable therefore `sb.append(" World!");` will append `" World!”` to same object. 
* Therefore, `System.out.println(sb);` will print out `"Hello World!”.`
---

